interface:
  display_name: "适应症拆分与回填"
  short_description: "校验完整分子池，拆分适应症并生成可追溯公式回填底稿"
  default_prompt: "Use $split-indications-and-backfill to inspect my complete Excel pool, preview the indication split, and generate the verified formula-backfilled workbook after approval."
